"""Mock API contract routers (Story 1.3).

These return static, deterministic mock responses so the frontend and AI teams
can build against a signed-off contract before the real persistence and
pipeline logic land. Every path here mirrors `proposalai-frontend/lib/api`.
"""
